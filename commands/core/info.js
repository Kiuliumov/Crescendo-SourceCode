const { messageLink } = require("discord.js");

module.exports = {
    name: 'info',
    description: "Info command",
    async execute({ client, inter }) {
message.channel.send("Error code: 420")
      
    },
};